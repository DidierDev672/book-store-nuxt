/**
 * @use https://prettier.io/docs/configuration
 * @types {import("prettier").config}
 */

const config = {
    trailingComa: "es5",
    tabWidth: 4,
    semi: false,
    singleQuote: true
}

export default config;